let lado = 5;

const area = (lado) => 2 * lado;
const perimetro = (lado) => lado + lado + lado + lado;

console.log("Área del cuadrado: " + area(lado));
console.log("Perímetro del cuadrado: " + perimetro(lado));
